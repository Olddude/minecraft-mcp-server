{
  "servers": {
    "minecraft": {
      "command": "npm",
      "cwd": "${workspaceFolder}",
      "type": "stdio",
      "args": [
        "run",
        "start:backend:debug"
      ],
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "MCP_TRANSPORT": "stdio"
      }
    }
  }
}
