{
  "servers": {
    "minecraft": {
      "command": "npm",
      "type": "stdio",
      "cwd": "${workspaceFolder}",
      "args": ["run", "start:backend:debug"],
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "MCP_TRANSPORT": "stdio"
      }
    }
  }
}
