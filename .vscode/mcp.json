{
  "servers": {
    "minecraft": {
      "command": "npm",
      "cwd": "${workspaceFolder}",
      "args": [
        "run",
        "start:backend:debug"
      ],
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "PATH": "${workspaceFolder}/dist/bin:${env:PATH}"
      }
    }
  }
}
